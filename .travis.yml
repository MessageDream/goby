
language: go

os:
  - linux
  - osx
  
sudo: required

go:
  - 1.8.1

before_install:
  - mkdir -p $GOPATH/bin
  - curl https://glide.sh/get | sh

install:
  - glide install

script: 
  - go test $(glide novendor)
  - go build

deploy: off