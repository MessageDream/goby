language: go

go:
  - 1.8.1

before_install:
  - echo -e "\n" | sudo add-apt-repository ppa:masterminds/glide 
  - sudo apt-get update
  - sudo apt-get install glide

install:
  - glide install

script: 
  - go test $(glide novendor)
  - go build

deploy: off